# 房贷报价 Reference 标签页：实施计划与逐项对照

版本：2026-09-05。范围：德国房贷报价的录入、原始数据复现、现有 HTML 计算结果对照。

## 1. 交付状态与使用方式

本次已经生成并准备发布 `房贷比较器_Reference.html`：保留原有六个标签页，在最后加入第七个 **Reference · 报价参照**。本文件记录实际审阅结果、实现选择、验算依据、发布方式与回滚步骤；`prompt.md` 是可独立交给代码助手的任务说明。

**用户已于 2026-09-05 明确要求将本版本 push 到公开仓库 `wgchen25-droid/Finanierzung_Calculator` 的 `main`。** 发布时更新五个压缩 payload，并同时提交 `计划.md` 与 `prompt.md`；可读的 `房贷比较器_Reference.html` 继续作为本地交付副本。原始 PDF、姓名、地址、联系方式、房产地址和 PIN 不入库。首次打开页面时进入 Reference，但主计算器中的工作方案仍为演示或浏览器恢复的数据，不会自动载入真实报价。

建议先在 Reference 选择银行，查看“PDF 对应哪个输入框”和“同一份报价，验算到分”，再使用“备份当前方案，并载入此报价”。载入一次只放入一家银行，另一家仍可在 Reference 中随时查看。载入前请求下载旧方案 JSON，并保留本页面会话内的一次撤销；刷新页面后撤销记录失效。浏览器可能拦截下载，重要数据仍应先手工导出并确认文件落盘。

## 2. 依据与证据边界

### 2.1 已读取的代码

- 仓库：`wgchen25-droid/Finanierzung_Calculator`。
- 分支：`main`。
- 审阅提交：`9c1e0c96eea2054db662b4d1f2d0b1c54e34af9c`。
- 原始入口：`index.html`，1,872 字节。
- 实际页面：`assets/payload-1.js` 至 `assets/payload-5.js` 内的连续 Base64 + gzip 数据；解压后是 86,610 字节、578 行的单文件 HTML。

入口通过 `DecompressionStream('gzip')` 解压，然后执行 `document.open()`、`document.write(html)` 和 `document.close()`。因此，在外层 `index.html` 末尾追加普通标签页会在加载时被替换。必须修改解压后的实际页面，或用更新后的完整单文件 HTML 替代入口。

五个 payload 已逐一校验 Git blob SHA，拼接后的 gzip 解压及校验成功。以下代码位置均指**该提交解压后的版本**，不指只有加载代码的外层 `index.html`：

| 位置 | 实际职责 | 本次处理 |
|---|---|---|
| `loanNeed()`，第 111 行 | 共同预算减实投资金，得到贷款需求 | 不改公式 |
| `basePayment()`，第 119 行 | 用原始月供，或由本金、Sollzins、Tilgung 算月供 | 不改公式 |
| `validate()`，第 123 行 | 分笔金额与共同贷款需求差额超过 1 € 时拒绝比较 | 保留，不能为两家报价把容差改成 50 € |
| `simulateOffer()`，第 156 行 | 按月计息、分取整、特别还款与续贷 | 不改业务逻辑 |
| `sale()`，第 216 行 | 卖房现金流；直接使用共同 `global.equity` | 不增加不完整的银行资金覆盖逻辑 |
| `normalize()`，第 240 行 | v1 JSON 校验、字段白名单与 ID 重映射 | 追加 Reference 标签和可选 UI 选择状态 |
| `buildGlobalFields()`，第 319 行 | 共同购房与情境输入框 | 保留实际字段，不另造一套表单名 |
| `buildOfferEditors()`，第 327 行 | 银行及分笔编辑器 | 用实际字段建立参考映射 |
| `renderBanner()`，第 358 行 | 演示／手工输入提示 | 区分“工作数据仍为演示”与“参考页含报价摘录” |
| `setTab()`，第 362 行 | 切换页面及 ARIA 状态 | 扩充 `reference` 白名单 |
| `renderSchedule()`，第 468 行 | 按模拟年汇总 | 原页不变；Reference 另按 PDF 日历区间汇总 |
| `recalc()`，第 477 行 | 计算并刷新页面 | 追加 Reference 的实时差额刷新 |
| `rebuild()`，第 481 行 | 重建录入页并刷新结果 | 沿用，不建立旁路工作状态 |

现有六个 tab 为 `compare`、`inputs`、`sale`、`refi`、`schedule`、`help`。第七个使用 `reference`，不要与已有的续贷页 `refi` 混淆。

### 2.2 已读取的报价

来源：用户上传的 21 页融资建议 PDF，日期 **2026-09-01**。为避免在开发资料中传播直接身份信息，不重复申请人姓名、文件原名、住址、房屋地址、联系方式或 PIN。

来源 SHA-256：

```text
4f64c61c5c781882bd69d144bd4c7c316803613b785743ef9a00e5ab9f74582c
```

| 内容 | PDF 页码 |
|---|---|
| 购房预算、可用／投入资金及基础贷款需求 | 3 |
| Commerzbank 方案条款 | 5 |
| Commerzbank 锁息期内还款表及累计值 | 8–9 |
| ING 实际投入资金及方案条款 | 12 |
| ING 锁息期内还款表及累计值 | 15–16 |
| 还款路径为示例的说明 | 10、17 |
| 非约束性、资料提交及选择日期 | 4、11、19 |

原件注明该建议尚非有约束力的贷款要约，须进一步审核，并要求在 **2026-09-03** 前提交资料。Reference 只能表示这份文件的历史快照，不能表示目前仍可申请、利率仍有效、银行已审批或未来续贷条件已锁定。

本次未使用网络市场利率替换原件数据，也未根据一般产品知识补写原件未提供的条款。

## 3. 原件数字：先建立不能被工作输入改写的基准

### 3.1 共同购房预算

来源：PDF 第 3 页。单位：EUR。

| 原件名称 | 数值 | 对现有计算器的意义 |
|---|---:|---|
| Kaufpreis | 750,000.00 | `global.price` |
| Grunderwerbsteuer | 6.50% / 48,750.00 | 合并到 `global.buyPct` |
| Notargebühr | 1.80% / 13,500.00 | 合并到 `global.buyPct`，保留原件名称 |
| 附加成本合计（本次相加） | 8.30% / 62,250.00 | 不要把 8.30% 误称为单独的购房税率 |
| 项目总额 | 812,250.00 | 750,000 + 48,750 + 13,500 |
| 可用 Bank- und Sparguthaben | 260,000.00 | “可用”与各银行“实际投入”分开 |

原件没有单独列出中介费、登记费、装修费等其他预算项。为复现这份预算，把 `buyFixed` 与 `renovation` 设为 0，但界面必须标记为**复现设置／原件未另列**，不得解释成实际费用已确认为零。原件只写 `Notargebühr`，不能自行断言 1.80% 已包含全部 Grundbuch 费用。

另一种等价预算输入是 `buyPct=0, buyFixed=62250`；本版统一采用 `buyPct=8.3, buyFixed=0`，避免两者同时计入导致重复加费。

### 3.2 两家报价的输入与合同记录

来源：Commerzbank 第 5 页；ING 第 12 页；Commerzbank 资金来源结合第 3 页。

| 项目 | Commerzbank | ING |
|---|---:|---:|
| 可用自有资金 | 260,000.00 | 260,000.00 |
| **实际投入自有资金** | **260,000.00** | **259,950.00** |
| **贷款本金 / Nettodarlehensbetrag** | **552,250.00** | **552,300.00** |
| Gebundener Sollzins | 3.86% | 3.89% |
| Effektiver Jahreszins | 3.95% | 3.99% |
| Monatliche Gesamtrate | 2,696.82 | 2,710.87 |
| 初始 Tilgungssatz | 2.00% | 2.00% |
| Zinsbindung | 10 年 | 10 年 |
| Kalkulierte Laufzeit (ca.) | 约 28 年 | 约 28 年 |
| 放款日 | 2026-09-30 | 2026-09-30 |
| 首次还款日 | 2026-10-31 | 2026-10-31 |
| 资金预备利息免计期 | 6 个月 | 6 个月 |
| 第 7 个月起资金预备利息，原件为年率 | 3.00% / 年 | 2.40% / 年 |
| **换算为现有输入框需要的月率** | **0.25% / 月** | **0.20% / 月** |
| 选定方案的可选 Sondertilgung | 0.00% / 年 | 5.00% / 年 |
| 为复现原表实际执行的额外还款 | 0 | 0 |

ING 多借的 50 € 来自少投入的 50 € 自有资金，不是从利息里产生的误差，也不能未经原件支持称作手续费。

### 3.3 条款不能简化掉的部分

Commerzbank 的选定方案栏写 `Optionale Sondertilgung 0.00%`；同页一般产品文字另外描述“全额放款满一年以后，在 6 月 30 日，至少 1,000 €”。本案例录入 `specialPct=0`，并保留**一般文字的适用范围待确认**，不能据此自动增添特别还款权利。（第 5 页）

ING 可在一个日历年内多次额外还款，合计至原始贷款的 5%，每次至少 1,000 €。本次换算额度为 `552300 × 5% = 27615 €`。这只是允许的上限，原始还款表没有执行该金额；实际预算仍为 0。续约时 5% 的计算基数依选择的 Leistungsbasis 而定，不能自动永远使用原始本金。（第 12 页）

ING 在每个锁息期内最多两次免费调整 Tilgung，范围 1.25%–10%；更多次数有另外费用，但原件未列该费用金额。最多 25,000 € 可不提取部分贷款，费用为不提取金额的 1%，并须满足最低贷款金额；该上限针对总融资，不是每个账户。Reference 只记录，不自动改变月供、不预先从本金减掉 25,000 €，也不强制新增 250 € 费用。（第 12 页）

## 4. 如何填入现有 HTML：完整映射

`g` 表示 `state.global`，`o` 表示某个 `state.offers[]`，`t` 表示该方案的一笔 `o.tranches[]`。实际 DOM 使用 `data-g`、`data-offer`、`data-tranche`、`data-key`；自动化不得依赖每次重建都会变化的 `f-数字` 输入 ID。

| 当前页面输入 / 实际字段 | Commerzbank | ING | 填写说明 |
|---|---:|---:|---|
| 成交房价 `g.price` | 750000 | 750000 | PDF p.3 |
| 购房附加成本 % `g.buyPct` | 8.3 | 8.3 | 6.5 + 1.8；不是税率本身 |
| 附加固定成本 `g.buyFixed` | 0 | 0 | 复现设置，不重复加 62250 |
| 装修／家具 `g.renovation` | 0 | 0 | 原件未另列；不是实际零成本确认 |
| 投入自有资金 `g.equity` | 260000 | 259950 | ING 必须使用实际投入值 |
| 月供比较方式 `g.compareMode` | `contract` | `contract` | 按原报价；不是 `equal` |
| 统一月供 `g.sameMonthly` | 不生效 | 不生效 | 原报价复现不要从此框计算月供 |
| 每年实际额外还款预算 `g.specialAnnual` | 0 | 0 | 不能把 5% 权利当作每年实际付款 |
| 银行名称 `o.name` | Commerzbank | ING | 加报价日期便于识别 |
| 贷款金额 `t.amount` | 552250 | 552300 | 不强制改成共同本金 |
| 名义年率 `t.rate` | 3.86 | 3.89 | 用于计息 |
| 有效年率 `t.effective` | 3.95 | 3.99 | 只记录，不替代名义年率 |
| 锁息年限 `t.fixedYears` | 10 | 10 | 第 120 期仍在初始锁息内 |
| 还款参数来源 `t.basis` | `monthly` | `monthly` | 优先使用原件付款金额 |
| 合同基础月供 `t.monthly` | 2696.82 | 2710.87 | 原值保留到分 |
| 初始还款率 `t.tilgung` | 2 | 2 | 原值保留，monthly 模式下不主导计算 |
| 年度特别还款权利 `t.specialPct` | 0 | 5 | 与 `g.specialAnnual` 分开 |
| 最初只付息月数 `t.graceMonths` | 0 | 0 | 首期已还本金，绝不是 6 |
| 一次性融资费用 `o.fees` | 0，复现设置 | 0，复现设置 | 还款表费用栏为 0；不是全部合同费用确认 |
| 已知预备利息总額 `o.commitment` | 0，复现设置 | 0，复现设置 | 本案例不另加；原引擎不自动计算未提款费 |
| 预备利息免计月数 `o.freeMonths` | 6 | 6 | 条款记录，不是本金还款宽限期 |
| 预备利息月费率 % `o.commitmentRate` | 0.25 | 0.20 | 原件年率除以 12 |
| 提前还款补偿金 `o.penalty` | `null` / 未确认 | `null` / 未确认 | 原件未提供确定金额，不能猜成 0 |
| 调整还款条款 `o.flexibility` | 未列明确次数范围 | 两次免费，1.25%–10% | 只保存说明 |
| 日期、出处、限制 `o.notes` | 日期 + 页码 + 待确认事项 | 同左 | 使用无直接身份信息的说明 |
| 放款／首期／到期日期 | Reference 记录 | Reference 记录 | 原工作模型没有真实日期字段 |
| 约 28 年总期限 | Reference 记录 | Reference 记录 | 不填入 fixedYears 或 refiYears |
| 卖价、租金、持有成本、续贷率等 | 原件未给 | 原件未给 | 不能宣称来自报价；加载时保留工作设置并警告 |

输入框接受无千分位数字：`552250`、`2696.82`、`3.86`。不要把 PDF 的 `552.250,00 €` 原样粘贴进 `type=number`。本版没有新增能自动识别全部德式金额格式的导入解析器；需要此能力时，应作为独立增强，并测试逗号小数点、千分位及空白。

## 5. 实际验算结果与公式

### 5.1 现有模型足以准确复现锁息期内示例

保持原有逐月分取整逻辑，分别使用各银行正确的实投资金、贷款本金、名义年率与合同月供，无特别还款，计算 120 期。

| 10 年指标 | Commerzbank PDF | 原引擎重算 | ING PDF | 原引擎重算 |
|---|---:|---:|---:|---:|
| 月供 | 2,696.82 | 2,696.82 | 2,710.87 | 2,710.87 |
| 剩余本金 | 417,714.67 | 417,714.67 | 417,540.16 | 417,540.16 |
| 累计常规月供 | 323,618.40 | 323,618.40 | 325,304.40 | 325,304.40 |
| 累计利息 | 189,083.07 | 189,083.07 | 190,544.56 | 190,544.56 |
| 累计归还本金 | 134,535.33 | 134,535.33 | 134,759.84 | 134,759.84 |

来源：PDF p.5、8–9、12、15–16。重算来自审阅提交的 `MortgageCore.simulateOffer()`，非把 PDF 的剩余本金当作模型输出。

此外，两家各核对 **24 个 PDF 区间 × 4 项金额**（月供、利息、归还本金、期末余债），总计 **192 个原表金额项**；差额全部为 **0.00 €**。原始 PDF 没有逐月展示全部 120 期，所以这里不是声称核对了 240 条银行逐月记录。

### 5.2 可人工追踪的首期公式

现有输入框中的利率是百分数数值。忽略本例未执行的特别还款时：

```text
需求 = round2(price × (1 + buyPct / 100) + buyFixed + renovation − equity)
月供 = 合同月供，或 round2(本金 × (Sollzins + 初始 Tilgung) / 1200)
当期利息 = round2(期初余债 × Sollzins / 1200)
当期归还本金 = 月供 − 当期利息
期末余债 = round2(期初余债 + 当期利息 − 月供)
```

Commerzbank 首期：`round2(552250 × 3.86 / 1200) = 1776.40`；还本金 `2696.82 − 1776.40 = 920.42`；余债 `551329.58`。ING 首期：利息 `1790.37`；还本金 `920.50`；余债 `551379.50`。均与第 8、15 页首期行一致。

`Tilgung=2` 也能在这两份报价中算出相同的分位月供，但优先保留原件 `monthly`，更方便逐项审计。有效年率不参与该月度利息公式。

### 5.3 两家原报价相减，不等于完全同条件定价

以下均为本次根据 PDF 原值相减，方向固定为 **ING − Commerzbank**：

| 项目 | 差额 |
|---|---:|
| 贷款本金 | +50.00 € |
| 实际投入自有资金 | −50.00 € |
| 名义年率 | +0.03 个百分点 |
| 有效年率 | +0.04 个百分点 |
| 月供 | +14.05 € |
| 120 期付款 | +1,686.00 € |
| 120 期累计利息 | +1,461.49 € |
| 120 期累计归还本金 | +224.51 € |
| 第 120 期末余债 | −174.51 € |

不据此自动推荐某家银行：本金、资金投入和合同灵活性不同。若未来新增统一本金或统一月供对照，必须独立标为“标准化假设试算”，不能覆盖原始报价参考值。

## 6. 为什么原页面填两份后会遇到问题

### 6.1 共用自有资金与 50 € 差异

原模型只存在一个 `global.equity`。设为 260000 时，贷款需求是 552250，ING 552300 会因差额 50 被拒绝；设为 259950 时，贷款需求是 552300，Commerzbank 反而不匹配。

本次选择最小改动方案：Reference 分别构造两份独立、合法、忠实报价的输入，调用同一个原引擎；主计算器每次只载入一家。这样既保留原报价，又不损坏共同假设比较器。

**本版没有实现“在原报价对比页中同时放入两家不同实投资金方案”。** 这属于第二阶段模型扩展，而不是把一个新字段塞入某个对象即可完成：若增加银行级 `equityOverride`，必须同时改 `loanNeed`、校验、摘要、销售累计投入、租房机会成本、净资产对照、导入导出及相关测试。尤其原 `sale()` 直接读取 `g.equity`，不能遗漏。

### 6.2 年率、月率及免计期不是同一字段

`rate` 是名义年率；`effective` 是只读记录的有效年率；`commitmentRate` 是月费率。原 PDF 的资金预备利息是年率，因此本例需除以 12。

`freeMonths=6` 表示资金预备利息免计期。`graceMonths=0` 表示首期即归还本金。两者交换会造成实质性还款误差。

### 6.3 时间轴不能把日历年当贷款年

第 0 期是 2026-09-30 放款，不计入常规月供累计。第 1 期是 2026-10-31，第 12 期是 2027-09-30，第 15 期是 2027-12-31，第 120 期是 2036-09-30。

PDF 的 2028 年年度行对应模拟第 16–27 期；2036 年锁息期内只有第 112–120 期。原“还款明细”页第 1 年则汇总第 1–12 期。Reference 按原表各自的区间累加，保留月末日期，不改变计息规则为日计息。

### 6.4 10 年后的两套路径不同

现有引擎在第 121 期开始，根据用户的 `refiRate` 和 `refiYears` 重设月供；默认演示的续贷设置为 4.5%、20 年，不能称为原报价条款。

PDF 的长期示例分别到 2054-08-31（Commerzbank）和 2054-07-31（ING）。示例总付款／总利息分别是 902720.98／350470.98 和 904715.37／352415.37（第 9、16 页）。本页仅记录这些长期示例，并引用第 10、17 页的示例声明；不要求原引擎在不同续贷设置下仍与它们相等。

## 7. Reference 的页面与交互计划

当前本地实现已经包含以下六个区块：

1. **报价概览与选择。** 两家银行并排卡片，注明来源日期、页码、资金差异和历史快照性质；选择参考银行及主计算器工作方案。
2. **字段映射。** 中文名称、德文原词、真实代码字段、应填／应记数值、工作值、差异与说明。分清“PDF 原值”“由原值换算”“复现设置”“仅作记录”“未确认”。
3. **三方结果对照。** PDF 报价值、原始输入重算、主计算器当前方案；分别列重算差额和工作方案差额，金额固定两位小数，不用整数欧元掩盖小差异。
4. **24 个原表区间验算。** 按 PDF 日期区间核对四项金额，支持导出逐项验算 CSV。
5. **合同口径说明。** 特别还款、预备利息、还款率调整、未提贷款及日期／期限等差异，不伪装成引擎已经支持全部合同规则。
6. **来源及边界。** 页码、代码版本、长期示例以及公开披露提醒；不包含原件或直接身份信息。

选择银行只改变 Reference 的 UI 状态。只有用户确认载入才更改工作数据。加载工作报价前请求完整 JSON 备份；提供当前会话一次撤销。加载后清楚提示：卖房、租金、持有及续贷参数保留当前设置，不是来自 PDF。

多分笔工作方案的本金可比较合计，但不能把分笔平均利率和单笔银行报价直接配对。校验失败时显示原校验原因，结果保持空白；当前第 120 期已经进入续贷时明确标注不具备相同锁息口径。输入不同不自动归因为计算器错误。

## 8. 数据与实现分层

### 8.1 不变参考数据

在完整 HTML 内使用 `<script id="reference-data" type="application/json">` 保存无直接身份信息的报价摘录。`reference-data.json` 同时在交付包中提供，方便审计和测试；运行 HTML 不依赖该外部文件。

初始化时深冻结参考数据。工作方案使用克隆，不持有可改写参考对象的引用。银行参考只读；修改工作月供、删除工作方案或导入其他 JSON 都不能修改原始参考值。

### 8.2 复用原引擎，不另写竞争公式

`referenceOffer(q)` 构造银行对象，`referenceGlobals(q)` 提供真实预算与明确的复现设置；`referenceBenchmark(q)` 调用 `MortgageCore.simulateOffer(...,120)`。`referenceChecks(q)` 用累计差值形成每个 PDF 区间的月供、利息及本金合计。

原有从 `loanNeed()` 至 `sale()` 的业务逻辑保持不变。工作方案仍通过原 `recalc()` 计算。Reference 不是一张硬编码“计算结果”的展示表：原值作为预期结果，计算列必须真正运行原引擎。

### 8.3 向后兼容

保留 `schema=1`、现有全局字段、方案结构和缓存键 `finanzierung-offer-comparator-v1`。扩充 `normalize()` 与 `setTab()` 的 tab 白名单，并增加可选 `ui.referenceBank`、`ui.referenceOffer`；旧 JSON 缺少这些字段时自动选择默认值。

导入时工作方案 ID 会重建，应将参考工作方案选择按原位置正确重映射，不能保存悬空 ID。已有 `export-json`、`export-html`、CSV 导出及六个原页面必须继续可用。带数据 HTML 导出必须保留 Reference 的 DOM、脚本、参考 JSON 和新的 UI 状态。

## 9. 验收与本次验证

### 9.1 已执行

- Node 无第三方依赖回归：**237 项断言通过**，包括 192 项原表区间金额断言，以及关键总额、120／121 期、字段记录行为、原有 50 € 拒绝规则、v1 导入和隐私文本检查。
- 浏览器自动化：**32 项检查通过**。涵盖 7 个 tab、切换不改数据、取消载入、备份、单家载入、金额映射、独立基准不被工作修改影响、撤销、带数据 HTML 往返、v1 JSON 导入、键盘导航及 CSV。
- 390px 和 320px 视口检查：无页面级横向溢出；宽表在容器内横向滚动。
- 已查看桌面、结果表和手机布局截图。
- 测试流程没有未捕获 JavaScript 异常，没有浏览器网络请求。
- 已比对源码：`normalize()` 之前的原核心代码逐字节不变，未调整月度计息、本金递推或销售公式。

详细结果在交付包的 `tests/core_test_report.json` 与 `tests/browser_test_report.json`。

### 9.2 测试范围的明确限制

浏览器测试使用 Chromium 144.0.7559.96，通过 Playwright 的 `set_content` 执行完整页面及导出的 HTML。受测试容器的浏览器管理策略限制，直接导航到 `file://` 被阻止，因此**未在此环境完成双击本地文件的直接导航测试**；也未验证 Safari、Firefox、真实 GitHub Pages 部署、浏览器缓存跨会话恢复或打印排版的全流程。

无特别还款的 120 期示例已验算。逐日计息、分批提款、实际预备利息、ING 日历年多次特别还款及最低金额、Commerzbank 特定还款日、自动调整 Tilgung、10 年后长期合同路径都**未新增实现**，不得用基线通过暗示这些功能已符合银行合同。

### 9.3 后续手工验收

在自己的桌面浏览器打开 HTML，确认 7 个 tab 可用；先导出旧数据，再载入任一家。检查 Reference 上输入一致、基准和当前结果差额均为 0；更改月供后仅当前差额变化。保存带数据 HTML 后关页重开，并确认参考、工作参数和提示都保留。上传至部署环境前再检查缓存、移动端、导出及打印。

## 10. GitHub 更新与回滚计划

### 阶段 A：冻结审阅基线（已完成本地）

记录上述提交 SHA、入口／payload 文件和校验结果。若后续执行时远端 `main` 已更新，先审阅差异，不能直接硬重置或覆盖用户的新版本。

### 阶段 B：修改实际页面与加入参考（已完成本地）

在解压后的页面中新增 `tab-reference`、`page-reference`，扩充两个 tab 白名单，添加只读原值、独立重算、字段映射及受控载入。保持原业务核心和现有页面。

### 阶段 C：本地回归与交付（已完成）

交付 HTML、两个 Markdown、参考 JSON 和 Node 测试及报告。不把 PDF 原件放进仓库包。

### 阶段 D：发布到 GitHub（本次执行）

- 用户已明确确认公开 push 当前 Reference 中的去直接身份信息报价财务数字。
- 以 `9c1e0c96eea2054db662b4d1f2d0b1c54e34af9c` 为父提交，保持 `index.html` 加载器不变。
- 选择**保持现有压缩分发**：将当前完整 HTML 用确定性 gzip（`mtime=0`）+ Base64 重新切为恰好 5 个 `assets/payload-*.js`，让根页面直接加载新版 Reference。
- 同一提交增加 `计划.md` 与 `prompt.md`；本地交付仍保留可读的 `房贷比较器_Reference.html`，远端运行版本继续沿用仓库既有五段 payload 分发。
- 不把原始 PDF、姓名、住址、房产地址、联系方式、PIN 或其他直接身份信息放入仓库。
- 更新 `main` 只允许 fast-forward；若远端头在提交前已变化，停止并重新审阅，不强制覆盖。
- 推送后重新读取 `main`、至少一个 payload 与两个 Markdown，确认 Reference tab、两家报价数据与文档均已进入远端。

该仓库为公开仓库。即使没有姓名和地址，实际本金、自有资金和报价仍属于财务信息。**本次已通过用户随后明确的“push to github”指令确认公开提交当前 Reference 中的去直接身份信息财务数字。** 这项确认不扩展到原始 PDF、姓名、住址、房产地址、联系方式、PIN 或其他直接身份信息；这些内容仍不得公开入库。

回滚时将 `main` fast-forward 到一个基于旧树的新回滚提交，恢复原五个 payload，并保留或删除新增 Markdown／维护副本；不建议 force-push 改写公开历史。不要在外层加载页追加 tab，也不要只修改某段 Base64 以“凑出”结果。

### 安全解压示例：只还原文本，不执行 payload JavaScript

以下在已取得仓库副本的目录运行：

```python
from pathlib import Path
import base64
import gzip
import re

parts = sorted(
    Path("assets").glob("payload-*.js"),
    key=lambda p: int(p.stem.split("-")[-1]),
)
if not parts:
    raise SystemExit("未发现 payload 文件；先检查仓库是否已改用完整 HTML")
chunks = []
for file in parts:
    text = file.read_text(encoding="utf-8")
    match = re.search(r'\+\s*"([A-Za-z0-9+/=]+)"\s*;', text)
    if not match:
        raise ValueError(f"不支持的 payload 结构：{file}")
    chunks.append(match.group(1))
raw = base64.b64decode("".join(chunks), validate=True)
html = gzip.decompress(raw).decode("utf-8")
if "MortgageCore" not in html or 'id="page-help"' not in html:
    raise ValueError("解压结果不像预期的实际计算器，请人工审阅")
Path("src").mkdir(exist_ok=True)
Path("src/calculator.html").write_text(html, encoding="utf-8")
```

### 回归运行方式

解压交付包后，在包根目录执行：

```bash
node tests/reference-regression.cjs "房贷比较器_Reference.html"
```

无须安装 npm 包。成功时输出 `status: PASS`、断言数与两家银行的 120 期结果。若代码后续变化造成差异，应展示实际误差并调查，不要修改参考数值、偷改利率、删掉失败测试或提高容差掩盖问题。

## 11. 后续可扩展，但不是本次功能

下一阶段可以支持银行级实际投入资金、德式金额粘贴解析、日历化特别还款计划、提款时间表以及只在本地载入用户 PDF／JSON。每个扩展都应有独立假设标记和测试，保留不可变原报价，不让改进模型覆盖原件。

本次的核心目标已经落实为：**知道填哪里、解释为什么不能直接统一两家本金、验证现有计算能否复现原件，并让工作方案与参考例子在同一页面透明对照。**
