# 给代码助手的执行 Prompt：在现有房贷 HTML 增加 Reference

> **本次执行授权备注（2026-09-05）**：用户在生成本 Prompt 后明确要求“push to github”。因此，本次可以把当前 Reference 中不含直接身份信息的报价财务数字提交到公开仓库；该授权不包括原始 PDF、申请人姓名、住址、房产地址、联系方式、PIN 或其他直接身份信息。


把本文件作为完整任务说明使用。目标不是另做一个无关的贷款计算器，而是在现有项目中加入可操作、可验算的报价参考页。

---

## 你的任务

请审阅并修改 `wgchen25-droid/Finanierzung_Calculator` 的实际 HTML 应用，在现有六个标签页后增加第七个 **Reference · 报价参照**。

以以下 2026-09-01 融资建议中的 Commerzbank 与 ING 两份报价为例，完成德文字段到现有中文输入框的精确映射、PDF 原值与原引擎重算的核对、当前工作方案差额显示、受控载入，以及回归测试。

请实际产出可运行的 HTML 与测试，不要只给设计建议。不得静默修改核心公式来匹配报价，不得覆盖用户工作数据，也不得公开提交本例财务数据，除非用户明确确认了公开披露范围。

## 1. 先确认输入项目形态

审阅基线为：

```text
repository: wgchen25-droid/Finanierzung_Calculator
branch: main
commit: 9c1e0c96eea2054db662b4d1f2d0b1c54e34af9c
```

该版本的外层 `index.html` 只是约 1.8 KB 的加载器，真正应用存于 `assets/payload-1.js` 至 `payload-5.js`。它拼接 `window.__MORTGAGE_PAYLOAD`，Base64 解码、gzip 解压，再用 `document.write()` 替换页面。

必须先还原实际 HTML，不能直接把新页写在外层加载器末尾。不要通过执行未知 JS 来取得文本，用正则提取字符串，`base64.b64decode(..., validate=True)` 与 `gzip.decompress` 即可。

若用户给的是已经展开的 `房贷比较器_Reference.html`，先检测现有 `tab-reference`、`page-reference` 和 `reference-data`，进行审阅／修补，**不得重复增加同名 tab**。若远端提交已不同，保留新改动并说明与基线的差异，不要强制重置。

## 2. 已确认的原项目结构

原项目是单文件、原生 JavaScript/CSS 的离线应用，不需要 React、服务端、API 或构建框架。

现有 tab：

```js
['compare', 'inputs', 'sale', 'refi', 'schedule', 'help']
```

原核心导出为 `window.MortgageCore`，在 Node 环境支持 `module.exports`。实际函数包括：

```text
loanNeed(g)
basePayment(t,g,total)
validate(g,o)
simulateOffer(g,o,maxMonths=480)
sale(g,result)
normalize(input)
```

UI 内部有 `buildGlobalFields()`、`buildOfferEditors()`、`renderBanner()`、`setTab()`、`renderSchedule()`、`recalc()`、`rebuild()`、`download()` 和 `csv()`。工作数据是 UI 闭包内的 `state`；不要用一个独立全局“副本”伪装成正在编辑的工作状态。

原结构：

```js
state = {
  schema: 1,
  demo: true,
  global: {
    price, buyPct, buyFixed, renovation, equity,
    compareMode, sameMonthly, specialAnnual,
    refiRate, refiYears, refiSpecialPct,
    sellYears, sellPrice, sellPct, sellFixed, sellTax,
    ownerAnnual, rentMonthly, rentGrowth, rentStartup, investmentReturn
  },
  offers: [{
    id, name, fees, commitment, penalty,
    freeMonths, commitmentRate, flexibility, notes,
    tranches: [{
      id, name, amount, rate, effective, fixedYears,
      basis, tilgung, monthly, specialPct, graceMonths
    }]
  }],
  ui: {tab, horizon, saleOffer, scheduleOffer, chartYear}
};
```

输入控件靠 `data-g`、`data-offer`、`data-tranche`、`data-key` 绑定。不要依赖会变化的 `f-数字` ID。现有 JSON schema 为 1，缓存键为 `finanzierung-offer-comparator-v1`，请保持兼容。

## 3. 必须忠实保留的原始数据

来源是用户提供的 21 页融资建议 PDF，日期 2026-09-01。原文件明确尚非有约束力的贷款要约，需后续审核；原资料提交／选择日期为 2026-09-03。不得把参考页写成目前可申请或已获批的银行产品推荐。

来源对应：预算 p.3；Commerzbank 条款 p.5、还款表 p.8–9；ING 条款 p.12、还款表 p.15–16；示例性质 p.10、17；非约束性／提交日期 p.4、11、19。

### 3.1 项目与两家方案

以下 JSON 用于定义原始事实与明示复现设置。金额是欧元数值，所有百分比均以百分数填写，例如 3.86，不是 0.0386。

```json
{
  "quoteDate": "2026-09-01",
  "submissionDeadline": "2026-09-03",
  "project": {
    "price": 750000,
    "taxPct": 6.5,
    "tax": 48750,
    "notaryPct": 1.8,
    "notary": 13500,
    "total": 812250,
    "buyPct": 8.3,
    "availableEquity": 260000,
    "sourcePage": 3
  },
  "reproductionSettings": {
    "buyFixed": 0,
    "renovation": 0,
    "compareMode": "contract",
    "basis": "monthly",
    "specialAnnual": 0,
    "graceMonths": 0,
    "fees": 0,
    "commitment": 0,
    "penalty": null,
    "months": 120
  },
  "banks": [
    {
      "id": "cb",
      "name": "Commerzbank",
      "equity": 260000,
      "principal": 552250,
      "rate": 3.86,
      "effective": 3.95,
      "monthly": 2696.82,
      "tilgung": 2,
      "fixedYears": 10,
      "approxTermYears": 28,
      "disbursementDate": "2026-09-30",
      "firstPaymentDate": "2026-10-31",
      "fixedEndDate": "2036-09-30",
      "freeMonths": 6,
      "commitmentAnnual": 3,
      "commitmentMonthly": 0.25,
      "specialPct": 0,
      "residual": 417714.67,
      "paid": 323618.40,
      "interest": 189083.07,
      "repaid": 134535.33,
      "sourcePage": 5,
      "schedulePage": 8,
      "summaryPage": 9
    },
    {
      "id": "ing",
      "name": "ING",
      "equity": 259950,
      "principal": 552300,
      "rate": 3.89,
      "effective": 3.99,
      "monthly": 2710.87,
      "tilgung": 2,
      "fixedYears": 10,
      "approxTermYears": 28,
      "disbursementDate": "2026-09-30",
      "firstPaymentDate": "2026-10-31",
      "fixedEndDate": "2036-09-30",
      "freeMonths": 6,
      "commitmentAnnual": 2.4,
      "commitmentMonthly": 0.2,
      "specialPct": 5,
      "residual": 417540.16,
      "paid": 325304.40,
      "interest": 190544.56,
      "repaid": 134759.84,
      "sourcePage": 12,
      "schedulePage": 15,
      "summaryPage": 16
    }
  ]
}
```

`fees=0` 表示为复现给定还款表不额外追加融资费用，不是断言所有合同成本都确认为零。`buyFixed=0`、`renovation=0` 是按原预算未另列而作的复现设置，不是实际零费用确认。`commitment=0` 对应本示例不另加预备利息，不表示任何提款安排都会免收。补偿金保持未确认 `null`。

不要把 Notargebühr 改写为“已包含全部登记／Grundbuch 成本”，因为原件没有支持这种合并。售房、租房、持有成本、未来利率等未提供的数据不能伪装成原件事实。

### 3.2 核验锚点

| 指标 | Commerzbank | ING |
|---|---:|---:|
| 第 1 期日期 | 2026-10-31 | 2026-10-31 |
| 第 1 期月供 | 2696.82 | 2710.87 |
| 第 1 期利息 | 1776.40 | 1790.37 |
| 第 1 期本金 | 920.42 | 920.50 |
| 第 1 期末余债 | 551329.58 | 551379.50 |
| 第 12 期末余债，2027-09-30 | 541007.49 | 541054.94 |
| 第 15 期末余债，2027-12-31 | 538128.51 | 538174.78 |
| 第 120 期末余债，2036-09-30 | 417714.67 | 417540.16 |
| 120 期累计付款 | 323618.40 | 325304.40 |
| 120 期累计利息 | 189083.07 | 190544.56 |
| 120 期累计本金 | 134535.33 | 134759.84 |

以上来自 p.8–9 与 p.15–16。在审阅提交的原核心中，正确设置输入、每月按分取整、不执行特别还款，能重现这些值到分；不要为匹配而反解利率或手动覆盖余额。

### 3.3 PDF 年度区间：必须对齐，而非混用模拟年

每行顺序为“区间结束日期，期初月序号，期末月序号，期末余债，区间月供合计，区间利息，区间归还本金”。这是原 PDF 已显示的年度／不足年度行，未展示的逐月数据不得冒充银行原始行。

```json
{
  "cb": [
    ["2028-12-31",16,27,526331.17,32361.84,20564.50,11797.34],
    ["2029-12-31",28,39,514070.29,32361.84,20100.96,12260.88],
    ["2030-12-31",40,51,501327.69,32361.84,19619.24,12742.60],
    ["2031-12-31",52,63,488084.44,32361.84,19118.59,13243.25],
    ["2032-12-31",64,75,474320.86,32361.84,18598.26,13763.58],
    ["2033-12-31",76,87,460016.50,32361.84,18057.48,14304.36],
    ["2034-12-31",88,99,445150.12,32361.84,17495.46,14866.38],
    ["2035-12-31",100,111,429699.64,32361.84,16911.36,15450.48],
    ["2036-09-30",112,120,417714.67,24271.38,12286.41,11984.97]
  ],
  "ing": [
    ["2028-12-31",16,27,526370.35,32530.44,20726.01,11804.43],
    ["2029-12-31",28,39,514098.46,32530.44,20258.55,12271.89],
    ["2030-12-31",40,51,501340.59,32530.44,19772.57,12757.87],
    ["2031-12-31",52,63,488077.49,32530.44,19267.34,13263.10],
    ["2032-12-31",64,75,474289.15,32530.44,18742.10,13788.34],
    ["2033-12-31",76,87,459954.79,32530.44,18196.08,14334.36],
    ["2034-12-31",88,99,445052.77,32530.44,17628.42,14902.02],
    ["2035-12-31",100,111,429560.60,32530.44,17038.27,15492.17],
    ["2036-09-30",112,120,417540.16,24397.83,12377.39,12020.44]
  ]
}
```

若交付包中的 `reference-data.json` 可用，还应读取其中两家各 15 行逐月原始记录，加上以上 9 行，共各 24 个原表区间。该 JSON 已包含全部 48 个区间。只有本 prompt 时，以上锚点与 9 个年度区间足以运行关键回归，但不得声称自己验证了未取得的其他逐月原值；需要完整 24 行展示时取得随包 fixture 或从 PDF p.8、15 读取，不能由模型计算值反向填成 PDF 原值。

第 0 期放款不算作付款累计。PDF 中负数 Saldo 表示债务，页面可显示正的余债，但要说明符号转换。实际日期用安全的年月／月末映射，避免 `Date.setMonth` 从 31 日溢出到下月。

### 3.4 合同边界，必须出现在 Reference

- Commerzbank：选定方案可选 Sondertilgung 为 0%；同页一般产品描述另写满一年后 6 月 30 日、最低 1000 €。本例保持 `specialPct=0`，把一般描述的适用范围标为待确认，不自动扩张权利。
- ING：原始本金每日历年 5%，本例 27615 €；可以多次，每次至少 1000 €。额度与实际年度预算必须分开，复现时实际预算 0。续约基数依条款选择而变，不自动套用至全部未来年份。
- ING：每个锁息期最多两次免费调整初始还款率，范围 1.25%–10%；更多次数费用未列。仅记录，不自动改变还款计划。
- ING：总融资最多 25000 € 可不提，费用为未提金额的 1%，还须满足最低贷款额。仅记录，不能预先扣本金或自动收此费用。
- 两家资金预备利息免计期均为 6 个月；月率应为 0.25%／0.20%，不是把年率 3／2.4 直接填入月率字段。`freeMonths=6` 绝不是 `graceMonths=6`。
- 原引擎只在第 12、24…个月执行年度特别还款，尚不支持完整合同日历、最低单次金额及分批提款；不得夸大新增页支持范围。
- 10 年锁息和约 28 年预计总期限是两件事。第 121 期主引擎开始使用 `global.refiRate/refiYears`；这些未来设置不是报价。
- PDF 仅供记录的长期示例：CB 最后付款日 2054-08-31，总付款 902720.98、总利息 350470.98；ING 为 2054-07-31、904715.37、352415.37。不要让主引擎在任意续贷参数下强行匹配它们。

## 4. 关键架构决定：避免 50 € 共用资金冲突

原 `loanNeed(g)` 为：

```js
round2(g.price * (1 + g.buyPct/100) + g.buyFixed + g.renovation - g.equity)
```

原 `validate()` 对每个方案强制要求分笔本金合计与上述共同需求一致，绝对差超过 1 € 就报错。

两家实际自有资金不同，不能在原共同 `equity` 下同时直接通过：260000 对应贷款 552250；259950 对应 552300。**不许改贷款本金、不许忽略资金缺口、不许提高校验容差。**

本任务采用最小侵入方案：Reference 为每家构造独立且准确的 g/o，均复用 `MortgageCore.simulateOffer`；两家在 Reference 中并排展示与验算。载入主计算器时一次只载入一家，并明确替换影响。

本任务不要求在原“报价对比”页同时放入两家不同实际自有资金的报价。若要进一步实现，必须做完整银行级资金模型并覆盖 `sale()`、相对租房现金流、机会成本、净资产、校验与导入导出；不要只加 `equityOverride` 而让其余现金流仍引用共同资金。

## 5. 新页面的具体要求

页面 ID 为 `page-reference`；按钮 ID 为 `tab-reference`、`data-tab="reference"`，放在 `help` 后面，使用现有样式。两处 tab 白名单（`normalize()`、`setTab()`）均须更新，导入／导出不能丢掉新页。

Reference 至少包含：

1. 两家概览卡片、报价日期、非约束性及原资料日期提醒；注明本金与实投的 50 € 差别。
2. 参考银行选择，以及主计算器当前方案选择。
3. 德文字段到现有中文表单／真实代码字段的映射表，列参考值、当前值、状态及来源页码。
4. 三方计算表：PDF 原值、相同原始输入下的引擎输出、当前工作方案结果，分别显示两种差额。
5. 原还款表日期区间对照，四项金额及绝对误差；支持导出验算 CSV。
6. 合同记录、建模边界、来源及财务隐私提示。

状态要区分“原值”“换算”“复现设置”“仅记录”“未知”。不能把未知直接显示为 0，也不能显示“当前与参考不同，所以计算器错了”。数值差额显示到分，零差额归一为 0，避免 `-0` 误触发严格相等失败。

多分笔工作方案不得用平均利率冒充单笔原报价；字段无法一对一对应时说明。工作方案校验失败时展示实际原因，不用另一份有效基准的结果填到工作结果列。工作第 120 期已有续贷假设时，提示口径不可直接视为同一锁息期。

## 6. 字段映射必须覆盖

```text
PDF Kaufpreis                          → global.price
Grunderwerbsteuer + Notargebühr         → global.buyPct = 8.3
未另列附加固定成本 / 装修                → buyFixed=0 / renovation=0（复现设置）
实际投入 Guthaben                       → global.equity（260000 / 259950）
按原报价                               → compareMode='contract'
原始月供                               → tranches[].basis='monthly'
Darlehensbetrag                        → tranches[].amount
Sollzins                               → tranches[].rate
Effektiver Jahreszins                  → tranches[].effective（仅记录）
Monatliche Gesamtrate                  → tranches[].monthly
Tilgungssatz                           → tranches[].tilgung=2
Zinsbindung                            → tranches[].fixedYears=10
Optionale Sondertilgung                → tranches[].specialPct=0 / 5
原表实际额外还款                         → global.specialAnnual=0
首期已归还本金                          → tranches[].graceMonths=0
Bereitstellungszinsfreie Zeit           → offers[].freeMonths=6
Bereitstellungs-Zins 年率除以12          → offers[].commitmentRate=.25 / .20
本示例不另加已发生预备利息                → offers[].commitment=0（复现设置）
本示例不追加融资费用                     → offers[].fees=0（范围需确认）
未确认提前还款补偿金                     → offers[].penalty=null
调整还款率条件                          → offers[].flexibility / notes
放款日 / 首期日 / 固定期末 / 约28年       → Reference 记录，原工作模型无对应日期字段
```

`sameMonthly` 仅用于 `equal` 模式，不是录入原报价月供的地方。现有 `refiYears` 是锁息之后再完全还清的年数，不能填成“贷款约 28 年”。

输入格式至少给出正确例子：`552250`、`2696.82`、`3.86`，不要暗示原 `type=number` 已能解析 `552.250,00 €`。自动德式金额解析不是这次必须新增的功能；未实现就明确提示格式。

## 7. 数据保护和受控载入

把原报价放在独立、只读、可溯源的 fixture，例如内嵌 `reference-data` JSON。深冻结原数据，工作输入使用克隆。基准重算只用本银行真实预算和明确复现设置，不能混入主页面正在编辑的本金、利率或特别还款预算。

默认不自动改变工作方案。载入按钮必须：先明确提示会替换共同购房参数与全部银行列表；确认后请求下载完整旧方案 JSON；保留会话内一次撤销，再载入一个银行。取消确认不得修改金额、方案或本地缓存。浏览器无法保证下载落盘，告知用户核对备份，不能声称已永久保存。

保留当前卖房、租金、持有成本及续贷情境，明确显示它们不是 PDF 来源；不要把演示值改称真实报价参数。载入来源提示应能跟随方案 JSON／带数据 HTML 保存与恢复。`penalty=null` 表示未确认，不能推导赔偿为零。

可另提供“导出本报价录入 JSON”，不改工作状态；注明其中未提供的未来情境仍是当前设置。

保持 schema 1。新增可选 `ui.referenceBank` 与 `ui.referenceOffer` 时，确保旧文件缺少这些字段仍可导入，新选择随导出保留，ID 重建后不悬空。不得清空其他已有字段或悄悄更换缓存键。

不要把原 PDF、直接身份信息、PIN、联系方式、住址、房产地址或申请人姓名放入源码、fixture、日志、截图文档或公开提交。没有直接身份信息的财务数值也并非天然可公开；默认只交付本地改动，公开发布须获确认。

## 8. 计算与差额要求

复用原月末现金流模型、原有分取整规则。原业务计算逻辑无须为本例更改：

```text
月利息 = round2(期初余债 × 名义年率 / 1200)
本期本金 = 付款 − 月利息
期末余债 = round2(期初余债 + 月利息 − 付款 − 实际特别还款)
```

检查 120 期，不包含放款第 0 期的负支付，也不包含第 121 期续贷。年度区间用累计差值或逐期求和，都须按 PDF 的起止月份；用整数分对比或最终分位归一，不放大容差。

PDF 原值不能拿来填“模型计算值”；先运行引擎再比较。若不匹配，展示差额并调查输入、日期、取整、计息约定等可能原因，不能凭猜测断言银行算法。参考原值保持原样。

原报价之间的差额方向固定为 ING−Commerzbank：贷款 +50，自有资金 −50，月供 +14.05，120 期付款 +1686，利息 +1461.49，归还本金 +224.51，余债 −174.51。说明并非完全相同本金的纯利率比较，不自动给银行推荐。

## 9. 必须运行并报告的测试

对两家分别断言：项目预算及贷款需求正确；首期付款、利息、本金及余债正确；第 12、15、120 期余额匹配；120 期累计付款、利息及本金匹配；实际额外还款为 0；第 120 期不含续贷，第 121 期含续贷。

年度行必须按第 16–27、28–39……112–120 期核对。完整 fixture 可用时，对每家 24 个区间各核对 4 个金额，共 192 项。仅有部分锚点时，如实报告验证范围。

另外测试：

- 使用 `equity=260000` 配 ING 本金 552300，原校验仍拒绝；不通过提高容差让它“通过”。
- 更改 `effective` 不改变按名义年率的模拟结果；更改只作记录的 `freeMonths/commitmentRate` 不自动产生费用。
- ING `specialPct=5`、`specialAnnual=0` 时不能自动执行特别还款。
- 原引擎其他演示、多分笔及卖房计算没有因为新增参考页被改变。
- 查看或切换 Reference 不改变工作财务数据；取消载入不改变数据。
- 确认载入前备份正确；单家报价载入匹配；会话内撤销能回到旧方案。
- 修改当前月供后，仅工作结果／差额改变，原值和基准重算不变。
- 旧 v1 JSON 导入有效；新 UI 选择导出往返有效；保存带数据 HTML 后新 tab、原值及功能仍在。
- 未通过校验的工作方案不显示假结果；多分笔不误导；续贷口径不同有说明。
- 新 tab 仅一个，顺序最后，ARIA 对应、键盘可操作，320px／390px 不产生页面级横向溢出。
- 没有外部脚本、字体请求、追踪器、上传接口或未捕获 JS 错误；无直接身份信息泄露。

将实际执行命令、运行环境、通过数、失败项和未测范围写进测试报告。不要把人工推断写成已执行的浏览器测试，也不要声称已验证所有银行合同规则。

## 10. 文件、构建和最终交付

优先交付可读的完整单文件 HTML。可以将其作为新的 `index.html`；先保留原加载版本的回滚点，确认部署不再依赖旧 payload 后再清理。不要再只留无法直接维护的压缩字符串。

如必须保留原分发方式，提供唯一可读源 `src/calculator.html` 与可重复构建脚本；gzip 使用确定性时间戳，Base64 按块输出，入口根据实际块数生成引用。新增 Reference 后不保证仍为五块，须测试解压产物等于源文件，并处理过期块引用。

最终应提供：实际修改的 HTML、维护源／构建脚本（若适用）、无直接身份信息的参考 fixture、回归测试、测试报告及简短改动说明。实际附件已存在时不要重复造第二个数据源；保持参考数据与 UI 展示的来源一致。

最终回复明确说明：读取的是哪个提交、改了哪些文件、为何采用独立原报价验算、是否修改了核心公式、哪些检查通过、哪些未测试，以及是否真的推送／部署。没有远端写入就明确说没有；不要使用“已上线”描述本地文件。

验收底线：**用户看得懂每个报价数字应填哪里，原始 120 期可被原引擎真实复现，工作方案与参考不混淆，既有数据与功能不被破坏。**
